[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for client-side routes so React Router
# handles /privacy, /terms, etc. on direct load and refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
